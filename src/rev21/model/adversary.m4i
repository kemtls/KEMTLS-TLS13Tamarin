rule Reveal_Ltk:
  [ !Ltk($A, ~ltkA) ] --[ RevLtk($A) ]-> [ Out(~ltkA) ]

rule Reveal_PSK:
    [ SecretPSK($A, res_psk)]--[ RevealPSK($A, res_psk) ]->[Out(res_psk)]

// Reveal the KEM secret key
rule Reveal_KemSk:
    [ KemSk(~esk, ~tid, $A) ] --[ RevKemSk(~tid, $A, $ekem, ~esk) ]-> [ Out(~esk), KemSk(~esk, ~tid, $A) ]